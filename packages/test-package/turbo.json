{
	"$schema": "https://turbo.build/schema.json",
	"tasks": {
		"lint": {},
		"test": {
			"dependsOn": ["^build", "build"]
		},
		"build": {
			"dependsOn": ["^build"],
			"outputs": [
				"build/**",
				"dist/**"
			]
		}
	},
	"globalEnv": ["NODE_ENV", "CI"]
}
